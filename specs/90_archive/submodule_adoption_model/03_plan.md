# Plan

## 1. 当前步骤

1. 固化当前 `git clone` 现状
2. 回读旧 `Workstation Mode` / submodule 反对理由
3. 明确新的决策问题与比较方案
4. 输出正式决策文档
5. 输出 implementation spec
6. 落第一步实现
7. 落 `update` 观察层
8. 输出 pointer sync strategy
9. 输出 closeout
10. 如需继续，再拆出新主题

## 2. 本轮预期输出

- 一份新的 active 主题骨架
- 一份接入模型设计入口
- 一份明确的比较框架
- 一份首版决策文档
- 一份首版 implementation spec
- 一步最小实现落地
- 一步 `update` 观察层落地
- 一份 pointer sync strategy
- 一份 closeout

## 3. 当前不做

- 不直接把 installer 改成 `git submodule add`
- 不直接修改 init / update 用户手册
- 不直接修改 `.maglev/config.json` schema
