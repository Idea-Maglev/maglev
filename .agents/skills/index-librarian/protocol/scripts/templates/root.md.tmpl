---
type: entity-index
scope: root
entity_type: "{{entity_type}}"
index_protocol_version: "1.0"

child_count: {{child_count}}
stats:
{{stats_yaml}}

stats_schema:
{{stats_schema_yaml}}

sort_key: "{{sort_key}}"
sort_order: "{{sort_order}}"
child_type: "{{child_type}}"

{{#if table_columns}}
table_columns:
{{table_columns_yaml}}
{{/if}}

{{#if custom_checks}}
custom_checks:
{{custom_checks_yaml}}
{{/if}}

updated: "{{today}}"
---

# {{title}}

共 **{{stats.total}}** 个条目。

{{#if table_columns}}
## 索引

{{generated_table}}
{{else}}
## 索引

| 子目录 | 条目数 | 链接 |
|:---|:---|:---|
{{#each children}}
| {{name}} | {{stats.total}} | [→]({{path}}/) |
{{/each}}
{{/if}}
